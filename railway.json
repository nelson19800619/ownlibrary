{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && npm ci && npm run build && npx prisma generate && npx prisma migrate deploy || true"
  },
  "deploy": {
    "startCommand": "cd backend && npm start",
    "restartPolicyMaxRetries": 5
  }
}
